# Shareable template for RELAI simulator runtime environment variables.
# Copy to .relai/simulator.env and fill in real values locally; do not commit
# real secrets.

# Set at least one of the following two keys. OPENAI_API_KEY takes precedence
# over ANTHROPIC_API_KEY when both are set.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
