# RELAI simulator environment template. Copy to .relai/simulator.env and fill in
# values locally; do not commit real secrets. Set the provider you intend to use.
# When OPENAI_API_KEY is set it is preferred; otherwise ANTHROPIC_API_KEY is used
# via the litellm model path.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
